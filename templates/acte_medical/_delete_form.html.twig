<form method="post" action="{{ path('acte_medical_delete', {'id': acte_medical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acte_medical.id) }}">
    <button class="btn">Delete</button>
</form>
