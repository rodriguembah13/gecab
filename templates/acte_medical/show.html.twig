{% extends 'base.html.twig' %}

{% block title %}ActeMedical{% endblock %}

{% block body %}
    <h1>ActeMedical</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acte_medical.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ acte_medical.libelle }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ acte_medical.code }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ acte_medical.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('acte_medical_index') }}">back to list</a>

    <a href="{{ path('acte_medical_edit', {'id': acte_medical.id}) }}">edit</a>

    {{ include('acte_medical/_delete_form.html.twig') }}
{% endblock %}
