{% extends 'base.html.twig' %}

{% block title %}Antecedant{% endblock %}

{% block body %}
    <h1>Antecedant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ antecedant.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ antecedant.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ antecedant.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('antecedant_index') }}">back to list</a>

    <a href="{{ path('antecedant_edit', {'id': antecedant.id}) }}">edit</a>

    {{ include('antecedant/_delete_form.html.twig') }}
{% endblock %}
