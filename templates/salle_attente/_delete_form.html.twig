<form method="post" action="{{ path('salle_attente_delete', {'id': salle_attente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle_attente.id) }}">
    <button class="btn">Delete</button>
</form>
